# Automatic-RAG
Framework for Retrieval-Augmented Generation. Given a folder of documents, it automatically generates evaluation data, runs experiments to find the optimal RAG configuration (chunking, retrieval, reranking).
